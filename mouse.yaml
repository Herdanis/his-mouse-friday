agent:
  primary:
    provider: opencode
    model: default
  # secondary:              # fallback when primary unavailable
  #   provider: claude
  #   model: default
permissions:
  fs: {}
  commands: {}
a2a:
  allow_inbound: true
  allow_outbound: true
